@model IEnumerable<OnlineJudge.ViewModels.StandingRowViewModel>


@{
    ViewData["Title"] = "Standing";
}

<h1>Standing</h1>

<table class="table">
    <thead>
        <tr>
            <td>
                Handle
            </td>
            @if(Model.Count() > 0)
            {
                foreach (var row in Model.First().UserSubmitCount)
                {
                    <td>--</td>
                }
            }
            
        </tr>
    </thead>

    <tbody>
        <tr>
            @foreach(var row in Model)
            {
                if (!row.IsInContestTime)
                    continue;

                <td>@row.Handle</td>
                @foreach(var col in row.UserSubmitCount)
                {
                    <td>@col</td>
                }
            }
        </tr>
    </tbody>
</table>